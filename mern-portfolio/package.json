{
  "name": "mern-developer-portfolio",
  "version": "1.0.0",
  "description": "Production-grade MERN Stack Developer Portfolio with advanced features",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm start --workspace=server",
    "test": "npm test --workspace=client && npm test --workspace=server",
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server",
    "format": "npm run format --workspace=client && npm run format --workspace=server",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
